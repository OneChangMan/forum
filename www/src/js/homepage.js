/*
var path = window.location.pathname;
checkPageImOn(path);
*/

/**
 * Any idea why this doesn't work? Although it's quite a bad idea honestly.
 * Basically, I take a look at the href path of the webpage and addClasses accordingly.
 */
/*
function checkPageImOn(path) {
	if (path.includes('profile')) {
		$('#profile').addClass('active');
		return;
	} else if (path.includes('newpost')) {
		$('#newpost').addClass('active');
		return;
	} else if (path.includes('forum')) {
		$('#forum').addClass('active');
		return;
	}
}
*/